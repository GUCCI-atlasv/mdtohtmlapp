{
  "name": "mdtohtml-static-site",
  "private": true,
  "scripts": {
    "build:cheatsheet": "node build/gen-cheatsheet.js",
    "build:assets": "node build/stamp-assets.js",
    "build": "npm run build:cheatsheet && npm run build:assets",
    "test": "node --test test/*.test.js",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "dompurify": "^3.4.13",
    "jsdom": "^26.1.0",
    "marked": "12.0.0"
  }
}
